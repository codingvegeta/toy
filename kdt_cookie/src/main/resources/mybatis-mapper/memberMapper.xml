<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kdt_cookie.mapper.MemberMapper">

<insert id="signup" parameterType="com.example.kdt_cookie.member.dto.MemberDto">
    insert into customer
    ( id
    , pw
    , name
    , phone
    , address
    , email
    , gender
    , sns
    , birth
    , ismem
    , addressdetail
    , postcode)
    VALUES
        (  #{id}
           ,  #{pw}
           ,  #{name}
           ,  #{phone}
           ,  #{address}
           ,  #{email}
           , #{gender}
           ,  #{sns}
           ,  #{birth}
           ,  #{ismem}
           ,  #{addressdetail}
           , #{postcode}
           )
</insert>

    <select id="login" parameterType="com.example.kdt_cookie.member.dto.MemberDto" resultType="com.example.kdt_cookie.member.dto.MemberDto">
        select
            id
            ,pw
            from
            customer
            where id = #{id}
            and pw = #{pw}
    </select>


</mapper>